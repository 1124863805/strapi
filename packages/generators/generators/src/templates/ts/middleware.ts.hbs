/**
 * `{{ name }}` middleware
 */

import type { Core } from '@leao1/leao';

export default (config, { leao }: { leao: Core.Leao }) => {
  // Add your own logic here.
  return async (ctx, next) => {
    leao.log.info('In {{ name }} middleware.');

    await next();
  };
};
