import type { ComponentType } from 'react';
import { styled } from 'styled-components';

/* 表格样式由 ui/styles.css 提供，此处仅做结构封装 */
const Wrapper = styled.div``;

export const BoxWrapper: ComponentType<any> = Wrapper;
