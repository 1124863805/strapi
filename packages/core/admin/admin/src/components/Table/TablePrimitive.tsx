/**
 * Re-export of design-system Table (the scrollable table wrapper with rowCount/colCount).
 * Named TableWrapper to avoid conflict with admin's compound Table component.
 */
export { Table as TableWrapper } from '@leao1/design-system';
