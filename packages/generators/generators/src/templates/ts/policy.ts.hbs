/**
 * {{ id }} policy
 */

export default (policyContext, config, { leao }) => {
    // Add your own logic here.
    leao.log.info('In {{ id }} policy.');

    const canDoSomething = true;

    if (canDoSomething) {
      return true;
    }

    return false;
};
